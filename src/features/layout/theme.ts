import { createTheme } from '@mui/material/styles';

export const myTheme = createTheme({
  palette: {
    // type: 'dark',
    primary: { main: '#7A5D3D' },
    secondary: { main: '#7A5D3D' },
  },
});
